# @discord.self/docgen

Internal documentation generator used by this monorepo.

## Install

Workspace-only package.

## Example

```sh
pnpm --filter @discord.self/docgen build
pnpm --filter @discord.self/client docs
```

The package powers `docs.json` generation consumed by the website/docs pipeline.
