# @discord.self/scripts

Internal CLI scripts used by CI/docs/release workflows.

## Install

Workspace-only package.

## Commands

```sh
pnpm --filter @discord.self/scripts build
pnpm generate-split-documentation
pnpm sort-labels
```
