{
	"$schema": "https://json.schemastore.org/package.json",
	"name": "@discord.self/identity",
	"version": "0.1.0",
	"description": "Discord browser identity and httpcloak session management",
	"keywords": [
		"browser",
		"discord",
		"discordjs",
		"httpcloak",
		"identity",
		"rest"
	],
	"homepage": "https://discord.js.org",
	"bugs": {
		"url": "https://github.com/marioparaschiv/discord.self/issues"
	},
	"license": "Apache-2.0",
	"contributors": [
		"Mario P. <me@marioparaschiv.com>",
		"Mario P. <contact@marioparaschiv.com>"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/marioparaschiv/discord.self.git",
		"directory": "packages/identity"
	},
	"funding": "https://github.com/marioparaschiv/discord.self?sponsor",
	"directories": {
		"lib": "src",
		"test": "__tests__"
	},
	"files": [
		"dist"
	],
	"main": "./dist/index.js",
	"module": "./dist/index.mjs",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"require": {
				"types": "./dist/index.d.ts",
				"default": "./dist/index.js"
			},
			"import": {
				"types": "./dist/index.d.mts",
				"default": "./dist/index.mjs"
			}
		}
	},
	"publishConfig": {
		"access": "public",
		"provenance": true
	},
	"scripts": {
		"build": "tsc --noEmit && tsup",
		"build:docs": "tsc -p tsconfig.docs.json",
		"test": "vitest run --config ../../vitest.config.ts",
		"lint": "oxfmt --check . && oxlint --import-plugin --react-plugin --jsx-a11y-plugin --nextjs-plugin --jsdoc-plugin --vitest-plugin --promise-plugin --node-plugin --format=stylish src __tests__",
		"format": "oxfmt . && oxlint --import-plugin --react-plugin --jsx-a11y-plugin --nextjs-plugin --jsdoc-plugin --vitest-plugin --promise-plugin --node-plugin --fix --format=stylish src __tests__",
		"fmt": "pnpm run format",
		"docs": "pnpm run build:docs && api-extractor run --local --minify && generate-split-documentation",
		"prepack": "pnpm run build && pnpm run lint",
		"changelog": "git cliff --prepend ./CHANGELOG.md -u -c ./cliff.toml -r ../../ --include-path 'packages/identity/*'",
		"release": "cliff-jumper"
	},
	"dependencies": {
		"httpcloak": "^1.6.1",
		"tslib": "^2.8.1"
	},
	"devDependencies": {
		"@discord.self/api-extractor": "workspace:^",
		"@discord.self/scripts": "workspace:^",
		"@favware/cliff-jumper": "^6.0.0",
		"@types/node": "^22.19.15",
		"@vitest/coverage-v8": "^4.1.2",
		"esbuild-plugin-version-injector": "^1.2.1",
		"tsup": "^8.5.1",
		"turbo": "^2.8.21",
		"typescript": "~5.9.3",
		"vitest": "^4.1.2",
		"oxlint": "^1.59.0",
		"oxfmt": "^0.44.0"
	},
	"engines": {
		"node": ">=22.12.0"
	}
}
